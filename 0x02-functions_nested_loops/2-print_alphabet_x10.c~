#include "main.h"

/**
 * print_alphabet - prints all alphabets in lowercase.
 * 
 * Return: On success 1.
 */
void print_alphabet(void)
{
int i, size;
char l;
size = 10;
for (i = 0; i <= size; i++)
{
for (i = 'a'; i <= 'z'; i++)
{
_putchar(i);
}
_putchar('\n');
}
}

